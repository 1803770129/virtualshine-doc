import { useEffect } from 'react'
import { useRouter } from 'next/router'
interface INav {
    back: Boolean
}

export default function PdfLoader({ back }: INav) {
    return (
        <>
            <div>

            </div>
        </>
    )
}
